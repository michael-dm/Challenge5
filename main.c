#include <stdio.h>

int main(void)
{
	//Starting Challenge
	int step = 0;
	float time = 0.50; //the decimal places are the minutes for the real hour 

	printf("Ctrl+Maj+i");

	//URL found (easy)
	step = step + 1;
	time = time + 0.02;

	//01101000 ? 
	printf("Bin to Ascii");

	//binaryhexconverter.com
	step = step + 1;
	time = time + 0.03;

	printf("Kevin Mitnick");

	//Tsutomu Shimomura => Kevin Mitnick (luck?)
	step = step + 1;
	time = time + 0.07;
	
	printf("Automates");

	//wget https://fr.wikipedia.org/wiki/Expression_r%C3%A9guli%C3%A8re | regex101
	step = step + 1;
	time = time + 0.02;


	printf("Git");

	//YES
	return step;
}
